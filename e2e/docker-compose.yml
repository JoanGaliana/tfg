version: '3'
services:
  web:
    build: ../front
    image: front
    restart: always
    environment: 
      - API_URL=http://localhost:8080
    ports:
      - "3000:80"
  back:
    build: ../back
    image: back
    restart: always
    ports:
      - "8080:8080"